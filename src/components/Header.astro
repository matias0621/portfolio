<>
  <div class="fixed bg-blue-700 left-0 top-0 -z-10 h-full w-full">
    <div
      class="absolute top-0 z-[-2] min-h-full w-full bg-neutral-950 bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(10,119,198,0.3),rgba(255,255,255,0))]"
    >
    </div>
  </div>

  <header
    class="flex justify-center items-center flex-wrap py-6 w-full xl:w-[1120px] mx-auto"
  >
    <nav class="flex flex-row gap-x-10 opacity-80">
      <a
        href="#experience"
        class="text-slate-50 hover:text-blue-300 hover:scale-105 transition animate-zoom-in animate-delay-100"
        >Experiencia</a
      >
      <a
        href="#projects"
        class="text-slate-50 hover:text-blue-300 hover:scale-105 transition animate-zoom-in animate-delay-200"
        >Proyectos</a
      >
      <!-- <a
        href="#contact"
        class="text-slate-50 hover:text-blue-300 hover:scale-105 transition animate-zoom-in animate-delay-300"
        >Contacto</a
      > -->
    </nav>
  </header>
</>

<style>
  @keyframes zoom-in {
    0% {
      opacity: 0;
      transform: scale(0.5);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-delay-100 {
    animation-delay: 0.1s;
  }
  .animate-delay-200 {
    animation-delay: 0.2s;
  }
  .animate-delay-300 {
    animation-delay: 0.3s;
  }
</style>
